unit Click_Spectral3_config;

uses Click_Spectral3_types;

const
    _SPECTRAL3_TIMER_LIMIT : uint32_t = 5; // 5 ticks
    _SPECTRAL3_BUF_WARNING : uint16_t = 192; // 192 bytes activate warning
    _SPECTRAL3_POLL_ENABLE : uint8_t = 0; // poll enabled
    _SPECTRAL3_CALLBACK_ENABLE : uint8_t = 1; // calback disabled
    _SPECTRAL3_UART_CFG : array[1] of uint32_t = (115200); 
    
end.